<% if @user == current_user %>
  <h4><%= NormalizeCountry(@user.country.to_s,to: :short)%> <%= NormalizeCountry(@user.country, :to => :emoji)%></h4>
<% else %>
  <h4><%= NormalizeCountry(@user.country.to_s,to: :short) %> <%= NormalizeCountry(@user.country, :to => :emoji)%></h4>
<% end %>
